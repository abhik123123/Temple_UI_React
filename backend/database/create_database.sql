-- Step 1: Create database (Run this first in pgAdmin)
CREATE DATABASE templedb;

-- Step 2: After creating database, connect to it and run the rest
-- You can find the complete schema in backend/database/schema.sql
